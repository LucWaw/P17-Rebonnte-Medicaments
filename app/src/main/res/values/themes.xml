<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Rebonnte" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="theme_SignIn" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">@color/black</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/black</item>

        <!-- Contrôles UI -->
        <item name="colorControlNormal">@android:color/black</item>
        <item name="colorControlActivated">@android:color/black</item>
        <item name="colorControlHighlight">@android:color/black</item>
        <item name="colorButtonNormal">@color/black</item>

        <!-- Texte sur fond noir -->
        <item name="android:textColorPrimary">@android:color/black</item>
        <item name="colorOnPrimary">@android:color/black</item>
    </style>



    <style name="Theme.Rebonnte.ActionBar" parent="android:Theme.Material.Light.DarkActionBar" />

</resources>